{
  "name": "math",
  "version": "1.7.3",
  "description": "Add math syntax support to the editor and the preview",
  "keywords": [],
  "license": "MIT",
  "repository": "https://github.com/inkdropapp/inkdrop-math",
  "main": "./lib/index",
  "scripts": {
    "dev": "tsdown --watch",
    "build": "tsdown",
    "generate-css": "node scripts/generate-katex-css.js",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "format": "oxfmt",
    "prepublishOnly": "node --run typecheck && node --run lint && node --run generate-css && node --run build"
  },
  "dependencies": {
    "@matejmazur/react-katex": "^3.1.3",
    "katex": "0.17.0"
  },
  "devDependencies": {
    "@inkdropapp/types": "^0.0.8",
    "@types/node": "^26.0.1",
    "@types/react": "^19.2.17",
    "oxfmt": "^0.57.0",
    "oxlint": "^1.72.0",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3"
  },
  "engines": {
    "inkdrop": ">=5.6.0 <7.0.0"
  },
  "builtin": true,
  "styleSheets": [
    "katex.css"
  ]
}
