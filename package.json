{
  "name": "inkdrop-math-workspace",
  "private": true,
  "description": "Workspace for the Inkdrop Math plugin and the @inkdropapp/math rendering package",
  "license": "MIT",
  "repository": "https://github.com/inkdropapp/inkdrop-math",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "format": "oxfmt",
    "lint": "oxlint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "oxfmt": "^0.67.0",
    "oxlint": "^1.82.0"
  }
}
